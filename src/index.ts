// // 3d/src\index.ts
// // Exportação do componente React para a visualização 3D
// // export { ModelView } from "./components/ModelView";
// export { ThreeDScene } from "./components/3dScene";
// // export { ProjectScene } from "./components/ProjectScene";

// // Outros componentes ou utilitários podem ser exportados aqui futuramente

// // import { Vector3 } from "@zenith/math";

// // export class Shape3D {
// //   constructor(
// //     public position: Vector3,
// //     public size: number
// //   ) {}

// //   moveTo(newPosition: Vector3): void {
// //     this.position = newPosition;
// //     console.log(
// //       `Moved shape to position: (${newPosition.x}, ${newPosition.y}, ${newPosition.z})`
// //     );
// //   }

// //   resize(newSize: number): void {
// //     this.size = newSize;
// //     console.log(`Resized shape to size: ${newSize}`);
// //   }
// // }

// // export const drawCube = (position: Vector3, size: number): void => {
// //   console.log(
// //     `Drawing a cube at (${position.x}, ${position.y}, ${position.z}) with size: ${size}`
// //   );
// // };

// // export const drawSphere = (position: Vector3, radius: number): void => {
// //   console.log(
// //     `Drawing a sphere at (${position.x}, ${position.y}, ${position.z}) with radius: ${radius}`
// //   );
// // };
